{% extends "base.html.twig" %}

{% block body %}

    {% for info in data %}


        <div class="card" style="width: 18rem;">
            <img class="card-img-top" src="..." alt="Card image cap">
            <div class="card-body">
                <h1 class="card-title">Test Api </h1>

                <h2>{{ info.id }}</h2>
                <p>{{ info.nom }}</p>
                <p>{{ info.posteOuvert }}</p>
                <p>{{ info.typePoste }}</p>
                <p>{{ info.lon }}</p>
                <p>{{ info.lat }}</p>
                <p>{{ info.alt }}</p>
                <p>{{ info.postePublic }}</p>
                
                <a href="#" class="btn btn-primary">meteo de ton choix</a>
            </div>
        </div>

    {% endfor %}
{% endblock %}